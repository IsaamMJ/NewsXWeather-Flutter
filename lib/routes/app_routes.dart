abstract class AppRoutes {
  static const splash = '/';              // Splash screen route
  static const login = '/login';          // Login screen route
  static const signup = '/signup';        // Signup screen route
  static const otp = '/otp';              // OTP screen route (if applicable)
  static const home = '/home';            // Home screen route (if standalone)

  // Add route for Main Navigation (Bottom Navigation screen)
  static const mainNavigation = '/mainNavigation';  // Main Navigation screen route

  static const settings = '/settings';    // Settings screen route
}
